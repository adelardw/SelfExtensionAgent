# develop.md — рабочий план развития (ориентир при разработке)

Идейная основа — CLAUDE.md (замкнутый контур). Диагноз: самая тонкая стадия контура —
«↓ сигнал» (implicit feedback). Порядок A → B → C → D выбран так: A питает сигналом всё
остальное, B — главный материальный выигрыш парадигмы, C почти бесплатен, D требует
ключей и пары платных прогонов.

Жёсткие рамки (из CLAUDE.md, не нарушать): ядро заморожено (только few-shots/факты/навыки/
ParamStore); роли вслух не упоминать; онбординг по ходу исполнения; бюджет ~$15 — дорогие
прогоны только по явному слову; решения универсальны, не костыльны.

---

## ЭТАП ⓪ (2026-06-13): ОБУЧАЕМЫЙ ФРОНТ ГРАФА + GraphRAG ПАМЯТИ   🚧 PLANNED

**Диагноз (из сквозного чтения ядра).** Фронт графа `recall → reflexion` —
БЕЗУСЛОВНЫЙ и частично КОСТЫЛЬНЫЙ: (1) recall льёт память+AutoRAG в КАЖДЫЙ промпт;
(2) маршрут после LLM-reflexion перебивают русско-лексиконные регэкспы
(`_needs_web_grounding`/`_wants_physical_browser`/`_is_play_intent`, `agent.py:433,479,551`);
(3) граф памяти спит — `memory_edges` пишется (`agent.py:2097`), `neighbors`
(`store.py:279`) НЕ читается нигде; (4) few-shots reflexion берутся по топ-score, НЕ по
similarity (`prompt_store.py:181`); (5) `memory.embeddings:false` — recall на token-overlap.

**Принцип этапа.** Фронт делаем УСЛОВНЫМ и ОБУЧАЕМЫМ на эмбеддингах. Любое касание
АНТИ-ГАЛЛЮЦИНАЦИОННОГО пола — только за `_before_after_eval` accept/revert (доказательство
числом, не на веру). Асимметрия стоимости: маршрут вниз дёшев (сам эскалирует), вверх —
дорог → ВВЕРХ только по заземлённому рантайм-провалу, не по предсказанию.

**Нюанс производительности (определяет порядок).** `OpenAIEmbedder.embed`
(`embedder.py:96`) синхронный, блокирует loop. Включение эмбеддингов добавляет блок в
горячий путь recall → Thread 0 и Thread 1 приземляются ВМЕСТЕ.

### Thread 0 — эмбеддинги вкл + async-обёртка   ✅ СДЕЛАНО (2026-06-13)
`config.yml embeddings:true`; `embedder.py` += `aembed` (to_thread); `recall_node` зовёт
`recall_scored` через `asyncio.to_thread` (синхронный embed вне loop). КЛЮЧЕВОЙ ФИКС:
`recall` эмбеддил запрос на КАЖДЫЙ кандидат (`_relevance`) → N HTTP-вызовов; теперь qvec
считается ОДИН раз и прокидывается в `_rank_facts/_rank_episodes/_relevance/_score`.
Доказано: live embed 1536-dim/0.52с; тест `test_query_embedded_once` (ровно 1 вызов);
247 тестов зелёные, герметичны (fallback на token-overlap цел).

### Thread 1 — recall условный («recall не всегда»)   ✅ СДЕЛАНО (2026-06-13)
`recall_scored()` возвращает `(text, top_score)`; `recall()` — обёртка (обратная совместимость
для тула/тестов). Гейт `memory.recall_gate=0.22`: ассоциативная память (эпизоды/выводы)
инжектится только при `top_score ≥ gate`; персона-факты ВСЕГДА. Live: нерелевантный запрос →
только персона (score 0.000), релевантный → +эпизоды (0.429). Тесты: `tests/test_recall_gate.py`
(5 кейсов). Глубже — тулы `search_memory`/`recall_history` (не тронуты).

### Thread 2 — GraphRAG-lite (НЕ второй LightRAG)   ✅ СДЕЛАНО (2026-06-13)
Densify без LLM/сети: `add_fact._densify_fact` связывает новый факт с топ-3 фактов по
cosine≥0.6 (`fact↔fact similar`), сравнивая уже сохранённые векторы. Spreading-activation:
`_graph_boost` расходится от РЕЛЕВАНТНЫХ эпизод-сидов (rel≥graph_seed_min) по `neighbors`
(per-user!) с decay → boost фактов/выводов в `_rank_facts`+reflections. Конфиг
`graph_hops=1/graph_decay=0.6/graph_seed_min=0.3` (в конструкторе стора). Тесты:
`tests/test_memory_graph.py` (5). **PII-контейнмент доказан** (`test_graph_no_pull_when_query_irrelevant`):
нерелевантный запрос → эпизод НЕ сид → связанное НЕ тянется; рёбра per-user.
**ЧЕСТНО о ценности:** с включёнными эмбеддингами семантический recall САМ ловит
лексически-далёкое-но-близкое-по-смыслу → маржинальный выигрыш graph-boost только для
НЕ-семантических связей (факт, выведенный в релевантном эпизоде, далёкий по смыслу). Механизм
доказан детерминированно (`test_graph_boost_lifts_connected_fact`); live — без регресса.
ОТКРЫТО: graph-pull эпизодов вне top-k (сейчас boost только факты/выводы); fact-seeds (>1 хоп).

### Thread 3a — similarity-retrieved few-shots   ✅ ЧАСТИЧНО (2026-06-13)
СДЕЛАНО: `format_fewshots(query=…)` ранжирует примеры ПО ПОХОЖЕСТИ к запросу (kNN
маршрутизации), а не по голому score (было `prompt_store.py:181`). Дёшево — token-overlap
Jaccard (без сети в hot-path); персональные остаются приоритетнее глобальных, baseline-пол
сохранён. Подключено в reflexion (`query=state['query']`) и step_executor (`query=step.goal`).
Тест: `test_similarity_retrieval_ranks_relevant_first`. 263 passed.
ОТЛОЖЕНО (осознанно): embedding-kNN `IntentClassifier` для physical/play —
НЕ GAIA-релевантен (GAIA не про музыку/браузер), добавил бы embedding-вызов в hot-path;
регэксп там — high-precision энфорсмент, оставлен. Вернуться при фокусе на daily-ассистента.
`bandit.mode_prior` Jaccard→эмбеддинги — тоже отложено (текущий Jaccard работает).

### Thread 3b — анти-галлюцинационный пол (ТОЛЬКО за eval-доказательством)
Убрать дублирующий оверрайд `_needs_web_grounding→act` (`agent.py:433`); решение —
на модельном `ReflexionDecision.grounding`+GROUNDING_GATE; `_GROUND_RE` сузить до
high-precision СТРАХОВКИ. **Доказательство до коммита:** набор кейсов выдумки →
`_before_after_eval` (доля выдуманных URL/фактов ДО vs ПОСЛЕ), коммит только при after≤before,
иначе откат. Тест: расширить `tests/test_research_grounding.py`. **Риск ВЫСОКИЙ.**

### Thread 3c — heavy НЕ предсказывается, а ЗАРАБАТЫВАЕТСЯ   ✅ СДЕЛАНО (2026-06-13)
reflexion-авто `mode=='heavy'→'deliberate'` (heavy убран из стартовых; force_mode='heavy'
через /config уважается отдельным early-return). `_earned_review(state)`: сквозной deep-ревью
запускается ТОЛЬКО когда артефакт ОКАЗАЛСЯ большим — `len(final_answer)≥REVIEW_MIN_ARTIFACT(1200)`
И done-шагов≥`REVIEW_MIN_STEPS(3)` И есть `goal_rubric` (всё дёшево, рантайм-evidence). Мисс-класс
вверх в heavy = самый дорогой баг (eval: 928k токенов) → платим за ревью по факту, не по догадке.
`route_after_synthesize` использует `_earned_review` вместо `mode=='heavy'`. Тест: `test_heavy_routing`
обновлён (earned/малый-артефакт/бюджет/force_mode). 263 passed.

### Thread 2c — анти-PII детерминированный пол («не разглашать» = близнец «не выдумывать»)   ✅ СДЕЛАНО (2026-06-13)
(a) GraphRAG-поверхность — закрыто в Thread 2 (PII-контейнмент: нерелевантно→не тянем, per-user).
(b) `collective.maybe_promote` → `redact_pii(query)` ПЕРЕД промоушеном (email/телефон/карта не
утекают к другим юзерам; матчинг на оставшихся токенах). (c) `strip_ungrounded_pii` в финале
synthesize/act/fast/reason: EMAIL, которого нет в запросе+находках+памяти, → «[контакт удалён]»
(выдуманный контакт = галлюцинация; легитимный recall email юзера — не режем, т.к. память в
grounded). **АНТИ-РЕГРЕСС:** режем ТОЛЬКО email (плотные числа/телефоны НЕ трогаем) — числовые
GAIA-ответы целы (доказано `test_numeric_answer_untouched`). Тесты: `tests/test_pii_floor.py`
(6), 258 passed. ОТКРЫТО (как в ARCH TODO): LLM-генерализация рецептов — глубже redact.

### Thread 4 — маскинг контекста («забывание выполненного»)   ✅ СДЕЛАНО (2026-06-13)
(1) within-step: `_mask_old_tool_msgs` в `_exec_direct` сворачивает старые ToolMessage (кроме
последних `MASK_KEEP_TOOLMSGS=4`) в заглушку — анти-квадратичность длинной direct-цепочки;
tool_call_id и парность с AIMessage СОХРАНЯЮТСЯ (структура цела), идемпотентно, короткие (<80)
не трогаем. `create_agent`-research НЕ хачим (хрупко) — он уже маскирован структурно
(`agentic_research`: копятся только проверенные факты). (2) `prior_steps` адаптивен: последние
2 шага по 600 симв (межшаговая зависимость — список из шага N фильтрует N+1), старые по 200
(свёрнуты). РЕЗУЛЬТАТ шага (не ход мысли — он и так не сохраняется) идёт вперёд. Финал-валидация
всего (rubric) — уже есть. Тесты: `tests/test_context_masking.py` (4: свёртка/noop/идемпотентность/
короткие), 262 passed.

### Порядок
(0+1) вместе → 2 → 2c → 4 → 3a → 3c → 3b (последним, за eval). Каждая нить: ветка от main,
свои тесты + полный suite (242) + `bench_scenarios.py` на регресс (Thread 4 — особо на
токен-экономию и сохранность межшаговых данных). ОТКРЫТО: PII-генерализация рецептов перед
`collective.maybe_promote` (privacy в мульти-юзере).

---

## A. Единый журнал взаимодействий → настоящий implicit feedback   ✅ СДЕЛАНО (2026-06-12)

**Идея.** Схема контура: сигнал рождается из подтверждений действий, выбора пунктов плана,
HITL, уточнений. Эти события УЖЕ происходят (hitl.py, clarify-реестр, user_blocked), но
умирают в конце прогона. Сейчас implicit feedback = только эвристика [neg] по следующему
сообщению.

**Что сделать.**
1. Журнал событий взаимодействия за прогон (interaction ledger): HITL-разрешения/отказы
   (с описанием действия), ответы на уточнения (вопрос→ответ), user_blocked.
2. `reflect` пишет события в эпизод (вместе с trajectory).
3. Harvest превращает события в персонализацию:
   - HITL-отказ → факт профиля («не делать X без спроса») + few-shot осторожности;
   - clarify-ответ → факт профиля (онбординг-по-исполнению становится НАКОПИТЕЛЬНЫМ);
   - паттерн отказов → сигнал для per-user backward.
4. Ноль LLM-вызовов в горячем пути — всё в уже существующем reflect/harvest.

**Готово, когда:** события переживают прогон, видны в эпизоде, конвертируются в
факты/few-shots, покрыто тестами (отказ HITL → факт появился; clarify-ответ → факт появился).

**Реализация:** `src/interaction.py` (ledger-contextvar по образцу clarify + harvest без LLM);
`hitl.confirm` пишет решения (deny-by-default без канала НЕ пишется — это не выбор юзера);
эпизоды получили колонку `interactions` (JSON: HITL + clarify) — сырьё для B/C;
`recall` сбрасывает журнал, `reflect` сохраняет в эпизод и харвестит:
HITL-отказ → факт `hitl-отказ: <skill.tool>` (upsert — повторы не плодят дублей),
clarify-ответ (только answered) → факт `уточнение: <вопрос>`. Тесты: tests/test_interaction.py.

## B. Привычки: траектория → навык (само-расширение из повторов)   ✅ СДЕЛАНО (2026-06-12)

**Идея.** Само-расширение сейчас реактивное («нет способности → создать»). Парадигма
требует конвертировать ПОВТОРЯЮЩИЕСЯ дорогие deliberate-прогоны юзера в дешёвые
персональные навыки: «способность=цель, бюджет=констрейнт» + «универсальность = сумма
персонализаций» буквально.

**Что сделать.**
1. Reflect ищет k похожих успешных эпизодов юзера (векторная близость query+плана — уже есть).
2. Триггер дистилляции: план+шаги траекторий → параметризованный навык через существующую
   create_skill-ветку (smoke-тест обязателен).
3. Навык помечается как персональный; skill_selector подхватывает → задача этого типа у
   ЭТОГО юзера идёт быстрым/дешёвым путём.
4. Анти-оверфит: навык — обобщение k траекторий (параметры, не константы одного кейса).

**Готово, когда:** на синтетике (3 похожих эпизода) дистиллируется навык, проходит smoke,
выбирается selector'ом; тест без живого LLM (мок) + один живой прогон по слову юзера.

**Реализация (через существующие каналы, без новых LLM-путей и правки графа):**
`src/habits.py` + `MemoryStore.similar_successes`. Цикл: reflect после успешного
deliberate/heavy-прогона видит ≥HABIT_K (конфиг, дефолт 3) похожих успехов → пишет
факт-директиву «привычка: …» (importance 0.75) → recall инъектирует её в memory_context →
router (новое правило ИСКЛЮЧЕНИЕ-ПРИВЫЧКА в промпте) выбирает create_skill → существующая
ветка создаёт навык под всеми защитами (SGR-ревью+smoke) → reflect закрывает привычку (✅,
повторно не флагуется). В eval выключено (анти-оверфит). Дедуп кластера — Jaccard по ключу
факта. Платный шаг один и только в уже существующей ветке. Тесты: tests/test_habits.py.
Живой сквозной прогон (3 повтора → навык) — по явному слову юзера (бюджет).

## C. Per-user приоры выбора через бандитов   ✅ СДЕЛАНО для режимов (2026-06-12); selector — позже

**Идея.** Beta-Thompson на выбор режима reflexion / навыка / тула: ручка = (тип задачи →
режим/навык), reward = существующий validation-исход эпизода. Ноль LLM-затрат. Против
разреженности — та же двухъярусность, что у few-shots: глобальный prior + персональный
оверлей. Ядро не трогается: бандит крутит только ВЫБОР, не промпты.

**Готово, когда:** хранилище счётчиков (ParamStore-стиль), сэмплирование подмешивается в
reflexion/selector как prior (не диктат), деградация к текущему поведению при пустых
счётчиках, тесты на сходимость на синтетике.

**Реализация (режимы):** `src/bandit.py` + `MemoryStore.similar_episodes`. Хранилище
счётчиков НЕ понадобилось — эпизоды и есть счётчики (mode+outcome), нет второго источника
правды. Контекст ручки = кластер похожих запросов (Jaccard, как habits/feedback). Ключевая
ниша: few-shots харвестятся только из успехов — бандит добавляет НЕГАТИВНОЕ свидетельство
(«fast тут 0/4»). Beta(1+succ,1+fail) + Thompson → строка-прайор в существующий слот
memory_context reflexion (ядро не тронуто, прайор ≠ диктат). <3 наблюдений → '' →
поведение без изменений. Тесты: tests/test_bandit.py (сходимость, отсутствие утечки чужих
кластеров). Осталось из C: тот же прайор для skill_selector/ToolSearch (по полю skills
эпизода — данные уже пишутся) — взять после D или по слову юзера.

## D. Наследовать сильные MCP через measurable accept/revert   [очередь]

**Идея.** CLAUDE.md: «наследовать реально крутые MCP (вдруг уже есть сильный веб-поиск)».
Механизм уже есть — `_before_after_eval`. Применить к способностям: курируемый список
проверенных MCP (Tavily/Exa и т.п.) → при наличии ключа прогнать 3–5 кейсов против
собственного web_search → принять, если ИЗМЕРИМО лучше, иначе откат. Само-расширение
становится таким же измеримым, как само-улучшение.

**Готово, когда:** реестр кандидатов + сравнительный прогон + accept/revert; живой прогон —
только по явному слову (бюджет).

**Статус:** не начато. Сравнительная часть по определению живая (качество поиска не
оценить офлайн) → начинать, когда юзер даст слово на платный прогон; офлайн-болванка без
живой валидации рискует стать костылём.

---

## E. Браузер-первичность + act-режим + заземление действий   ✅ СДЕЛАНО (2026-06-12)

**Триггер:** живой прогон «Открой яндекс почту» — почта не открылась, контекст сгорел
(15k токенов), валидация приняла слова за дело. Диагноз по трейсам, три причины:

1. **Регрессия `_is_web`** (корень): веб-шаг определялся по `"search" in имя_тула`, а
   `search_memory`/`search_knowledge_base` прицеплены к КАЖДОМУ шагу → каждый шаг (даже
   «открой почту») шёл в тяжёлый agentic research вместо прямого действия. Фикс: `_web_step()`
   — по ВЫБРАННЫМ НАВЫКАМ, не по именам тулов. Регрессионный тест.
2. **Текст ≠ действие**: по-пунктовый валидатор не видел, вызывались ли инструменты.
   Фикс: в step-валидацию передаётся список РЕАЛЬНО вызванных тулов + правило «действие
   без вызова инструмента = не выполнено».
3. **Браузер-первичность** (решение юзера): интерактивный веб (открыть сайт/почту/кабинет,
   всё где нужны его логины) — через РЕАЛЬНЫЙ браузер юзера (`open_url` с конкретным URL
   сервиса + analyze_screen/type_text при необходимости); headless-поиск (search_web/fetch/
   deep_research) — ТОЛЬКО фоновый сбор информации. Зашито в step_execution, act-промпт и
   device_control.md.

**act-режим (6-й тип мышления, «System 1 с руками»)**: простое прямое действие → reflexion
выбирает act → BM25-подбор навыка по полным md (без LLM-селектора) → `_exec_direct` (1–2
вызова, HITL-обёртки сохранены) → reflect. Ни одного вызова тула или ESCALATE → эскалация
в deliberate через goal. Тяжёлый пайплайн — только когда прямого действия не хватает
(требование юзера). Тесты: tests/test_act_mode.py.

**Бандит под флагом**: `agent.bandit_prior` (конфиг, дефолт true) — юзер справедливо
напомнил, что это гипотеза; выключается одной строкой, критерий проверки — см. C.

## F. АРХИТЕКТУРНЫЙ ПРИНЦИП: «амортизированный агент»   [ядро ✅ 2026-06-12, доказательство — по слову юзера]

**Диагноз текущей архитектуры.** Она хорошо обучаемая (forward/backward по графу — это
реально отличительно), но как у ВСЕХ известных паттернов (ReAct, plan-execute, reflexion,
multi-agent) её предельная стоимость задачи ~ПОСТОЯННА: сотая похожая задача стоит столько
же LLM-вызовов, сколько первая. Фикс-пайплайн жжёт одинаковый оверхед на каждый прогон.

**Принцип (наше «принципиально новое», честно: новая КОМБИНАЦИЯ + измеримое утверждение).**
Каждый успешный прогон ОБЯЗАН оставить артефакт, делающий похожие задачи дешевле. Лестница
компиляции опыта: эпизод → few-shot → РЕЦЕПТ (план+навыки) → привычка → НАВЫК (код).
Плюс лестница исполнения с проверяемой эскалацией (вместо угадывания режима наперёд):
рецепт-replay → act → deliberate → heavy, ступень вверх — только по ПРОВЕРЕННОМУ провалу
(заземление: вызовы тулов, win/lose рецептов), ступень вниз — по компиляции опыта.
Это и есть «персонализация как метод» из CLAUDE.md, доведённая до архитектурного принципа:
агент СТАНОВИТСЯ дешевле и быстрее именно для своего пользователя.

**Измеримое утверждение (фальсифицируемое, дёшево проверяемое):** кривая стоимость/время
на задачу ПАДАЕТ с числом эпизодов при не худшем качестве. Проверка —
`scripts/amortize_bench.py`: один список задач × 2 прохода на одном user_id (cold vs warm);
тезис подтверждён, если pass-2 дешевле/быстрее при не худшей confidence. ПЛАТНЫЙ прогон —
только по явному слову.

**Ядро реализовано — РЕЦЕПТЫ (ступень-0):** `MemoryStore.recipes`
(add/find/get/feedback): успешный deliberate/heavy-прогон компилируется в рецепт
(план+навыки, дедуп по похожести); похожая задача (Jaccard ≥0.55) → селектор БЕЗ LLM-вызова
из рецепта, decompose получает проверенный план через слот capability_hint (шаблон ядра не
тронут); win/lose-трекинг, систематически проигрывающий рецепт самоудаляется.
Тесты: tests/test_recipes.py. Уже были: эскалация act→deliberate (E), привычка→навык (B),
бандит-прайор (C), few-shots (improve/) — рецепты замыкают лестницу.

**Живой прогон №1 (2026-06-12): тезис НЕ подтвердился** — warm +13% токенов, +19% времени
(cold 87k tok / $0.0103 / 268с / conf 78% → warm 98k / $0.0126 / 319с / 76%). Честный
разбор (отрицательный результат = данные):
1. ложный «переспрос слабого»: «саммари ARCHITECTURE.md» сматчился (Jaccard 0.35-гейт) со
   слабым «саммари README.md» → негативный сигнал заблокировал компиляцию рецепта и толкнул
   исполнителя «зайти иначе» (49с→130с, +14k tok). Фикс: _REPEAT_SIM 0.35→0.55;
2. рецепт экономил только селектор, а warm добавлял контекст в каждый вызов и НЕ скипал
   capability_research. Фикс: рецепт → скип capability_research (способность доказана);
3. дисперсия > дельты при n=4 несвязанных задач. Фикс: парные per-task дельты в бенче,
   семейства задач без лексических пересечений.
Прогон №2 — упал на транзиентном APIConnectionError (reason_node без защиты ронял весь
граф; бенч умирал целиком). Фиксы: reason_node crash-safe (пауза+повтор → честный отказ),
бенч переживает сбой одной задачи.

**Прогон №3: рецепты сработали на всех deliberate-задачах (селектор без LLM ×3,
capability_research скип), НО**: токены всё равно +12% (парно +5..+21%), а вот
НАДЁЖНОСТЬ выросла сильно — conf 76%→98%, задача-неудачник 18%→95%. Урок (ключевой
для принципа): **артефакт должен ЗАМЕНЯТЬ работу, а не аннотировать её** — хинты/few-shots/
прайор раздували контекст каждого из ~8 вызовов и съедали экономию двух дешёвых вызовов.
Фиксы: (а) sim≥0.7 → план берётся ИЗ рецепта, decompose БЕЗ LLM-вызова вообще;
(б) few-shots шага k=3→1 при рецепте. Прогон №4 — проверка.

**Прогон №4 (replace-not-annotate): ТЕЗИС ПОДТВЕРЖДЁН по токенам и качеству.**
cold 80.7k tok / $0.0101 / conf 78% → warm 70.2k / $0.0088 / conf 98%: **токены −13%,
качество +20 п.п.** Парно: рецептные задачи −19% и −26% tok; нерецептная reason-задача
+10% (на ней был сетевой повтор — но conf 18%→95%). Honest-оговорки: (1) n=4, нужна
серия для статистики; (2) ВРЕМЯ в этом прогоне хуже (+58% — латентность API/троттлинг
провайдера доминирует над числом вызовов; мерить медианой по повторам); (3) conf —
самооценка валидатора, не внешняя разметка. Итог трёх честных итераций: артефакты,
ЗАМЕНЯЮЩИЕ LLM-работу, дают и дешевле и надёжнее; аннотирующие — только надёжнее.

**Дальше (по слову юзера):** обратное направление лестницы (deliberate→act для задач с
устойчивым рецептом из 1 шага); рецепты в reflexion (старт со ступени рецепта, не с
предсказания режима); few-shots/прайор скипать при рецепте (ещё меньше тёплый контекст).

## G. Коллективные рецепты — «рекомендательная система поведения»   ✅ ядро (2026-06-12)

**Идея юзера:** автоматизация повторяющихся сложных задач — не только per-user: из многих
пользователей формируются best-practices внутри ассистента; похожим людям — примерно одно
и то же поведение агента (рекомендательная система: item=рецепт, фичи юзера=роли профиля).

**Реализация:** `src/collective.py`. Личный рецепт, доказавший себя (uses≥2, winrate≥0.66),
промоутится в общий пул инсталляции (`user_id='__global__'`) с отпечатком профиля источника
(значения роль-фактов). Новый юзер без своего рецепта получает коллективный: похожесть
запроса (≥0.55) + профиль-гейт (Jaccard≥0.08, мягкий: пустые профили пропускаются —
cold-start по запросу). Защиты: личное > коллективное; промоушен только проверенного
(один юзер не отравит всех); инъекционные запросы не промоутятся (не учиться на взломе);
win/lose глобальных копится дальше — не работающий у «похожих» рецепт самоудаляется
(дрейф тонет); в eval промоушена нет (анти-оверфит); пул в локальном SQLite, наружу не
уходит. Тесты: tests/test_collective.py.

**Дальше (по слову юзера):** LLM-генерализация при промоушене (личные параметры запроса →
плейсхолдеры; privacy в мульти-юзер деплое); тот же двухъярусный паттерн для привычек;
профиль-матчинг по эмбеддингам вместо Jaccard при появлении объёма.

## H. Физический веб «любой сайт / любая сущность / любой сервис»   [ПЕРЕФОКУС 2026-06-13: research-first]

⚠️ ПЕРЕФОКУС ПРОДУКТА (2026-06-13, решение юзера после дорогого флейла автозаполнения корзин
Я.Еды) — см. [[project-scope-research-first]]:
- ЯДРО (надёжно, оставляем): ПОИСК → отбор → АНАЛИЗ → РЕКОМЕНДАЦИЯ + ССЫЛКИ (headless web_search).
- ФИЗ-БРАУЗЕР = ТИХИЙ бонус: открыть страницу/лучший вариант в ФОНОВОЙ вкладке БЕЗ кражи фокуса;
  ответ концентрированный (юзер сделает TTS).
- МУЗЫКА/ВИДЕО — бонус для ПРОСТОГО (включить/открыть/плеер), не везде.
- ДОСТАВКА/ПОКУПКИ/КОРЗИНА/ЗАКАЗ — НЕ автономно (ресёрч+ссылки+открыть, оформляет юзер).
Причина: дешёвая модель не тянет надёжное вождение сложных коммерческих SPA; Puppeteer дал
видимость, но не вождение. Лучше надёжный узкий продукт. Puppeteer (Phase 0-2) остаётся
opt-in (/backend puppeteer) для тяжёлых SPA. Ниже H1-H6 — что УЖЕ работает (музыка/видео-плеер/
browse/анти-галлюцинация) под этой рамкой.

**Исходная цель (из /goal юзера, до перефокуса):** делать что угодно на любом физ-сайте через
любой сервис. Скорректирована: коммерцию-автономно убрали, ядро = поиск+рекомендации.

**Архитектура (готова):** расширение MV3 (`extension/`) ↔ WS-мост (`browser_bridge.py`) ↔
навык `browser_control`. Структурный снапшот DOM (пронумерованные элементы), клик/ввод/
медиа/чтение по номеру. Агент действует в РЕАЛЬНОМ браузере юзера (его логины), фоном.

**ПРИНЦИП READ-vs-PHYSICAL (уточнение юзера 2026-06-12, физ-браузер нужен НЕ всегда):**
- ЧИТАТЬ/ИСКАТЬ контент (найти инфу/список/обзоры/рейтинг, «что есть/какие варианты»,
  почитать страницы) → HEADLESS web_search/research (дёшево, анонимно, chunk-извлечение,
  физ-вкладки не трогаем). ДЕФОЛТ для «найди/покажи/перечисли» без логина и воспроизведения.
- ВИЗУАЛ / ВОСПРОИЗВЕДЕНИЕ / ПЕРС-ДАННЫЕ / ДЕЙСТВИЕ под логином (включи музыку/видео,
  открой своё избранное/кабинет/корзину, доставка, нажать/заполнить) → ФИЗ-браузер (browser_*).
Закодировано в act_system_prompt, step_execution, reflexion-гейт `_wants_current_web`.
Проверено: «найди обзоры X» (без сайта) → headless дёшево; «включи музыку X» → физ, играет.
ОСТАТОК: «перечисли точные тайтлы видео НА YouTube» всё равно тянет физ-браузер (нужны
актуальные тайтлы) → жжёт бюджет; кандидат на лёгкий снапшот выдачи / browse-режим (H6).

### H1. Музыка   ✅ СДЕЛАНО (2026-06-12), доказано вживую на ЯМ + Spotify
Кейсы через NL (`main.py "…" --auto`), все живьём: включить (реальный звук фоном),
избранное+перечислить, пауза/продолжить, анализ альбомов автора, подсказка вариантов.
Решённые блокеры старта звука (см. [[project-browser-control]]):
1. autoplay-policy → trusted-клик через CDP (chrome.debugger Input.dispatchMouseEvent +
   Runtime.evaluate userGesture) — как nanobrowser/puppeteer-in-extension;
2. видимость: скрытая вкладка (visibilityState=hidden) не инициализирует плеер → краткая
   активация вкладки с возвратом фокуса юзеру;
3. Spotify: спуф Visibility РВАЛ его DRM (фидбек юзера «работает при ручном переключении») →
   убран; окно инициализации EME 1.2с. Истина для Spotify = уши (его `<video>` muted, звук
   через Web Audio — element-вердикт ненадёжен);
4. profile picker → запуск Chrome с `--profile-directory=<last_used>`.

### H2. Де-хардкод «любой сервис»   ✅ СДЕЛАНО (2026-06-12, требование «не должно быть ликов о Яндексе»)
ВЫПИЛЕНЫ из CLI прямые костыли `/play /watch /open /pause /resume` (cmd_browser_direct +
_browser_ready удалены) — весь физический веб только через граф. Промпты де-якорены: выбор
сервиса = запрос → память (тег 'сервис') → уместный популярный, «не привязывайся к бренду».
Универсальная навигация к «моему X» (избранное/библиотека/корзина): КЛИК по пункту меню из
снапшота, НИКОГДА не угадывать URL (провал `users/yandex/playlists/liked-` → «ничего не
нашлось»). Сервисное предпочтение запоминается (memory_extraction, тег 'сервис').

### H3. Анти-галлюцинация на физ-вебе   ✅ СДЕЛАНО (2026-06-12)
Три guard'а в `agent.py` (все без LLM, общие):
- `_is_degenerate` (synthesize): вырожденный повтор («I'm Sorry» ×58 при неудачном чтении
  виртуализированного списка) → честный отказ, не мусор;
- `_is_false_access_refusal` (synthesize): «не имею доступа к аккаунтам» под давлением
  бюджета → честный статус (доступ ЕСТЬ через расширение);
- `_wants_current_web` (reflexion): «найди/покажи варианты» актуальной выдачи (видео/обзоры/
  товары/цены) при fast/reason → форс в act (грунт браузером; модель сочиняла 5 обзоров Sony
  XM5 с фейк-каналами). Тесты: tests/test_service_choice.py. 228 зелёных.

### H4. Видео / кино / аниме / сайты   [В РАБОТЕ]
Сделано вживую: «включи видео <X>» → YouTube, реальный звук фоном (Numb 4K); пауза видео ✅;
трейлер ✅ («Дюна 2 — трейлер» играет); «где посмотреть аниме X» → headless, реальные сервисы
с ценами (Crunchyroll/Кинопоиск); find-список видео-обзоров через browse (H6) — реальные
тайтлы, бюджет цел.
Сделано (продолжение): CMD_TIMEOUT моста 25→45с (тяжёлые стриминги типа Кинопоиска не
укладывались → TimeoutError); play_intent теперь покрывает фильм/сериал/аниме/трейлер/серию
(не только трек/клип) → честная проверка реального воспроизведения; ДЕТЕКТ СТЕНЫ ПОДПИСКИ/
ВХОДА (paywall) → чистый честный отказ «контент требует подписки/входа, могу трейлер или
поискать где бесплатно», БЕЗ клика-по-мусору и БЕЗ течи снапшота (живой баг: автоплей кликал
«Загрузить в Google Play», финал слил dedup-ноту/снапшот). Фолбэк `_exec_direct` чистит
плумбинг (dedup/ошибки/пустышки) — не утекает юзеру.
ФИЛЬМ ✅ ДОКАЗАНО (2026-06-12): «Однажды в Америке» → kinopoisk.ru поиск → клик «Смотреть
онлайн» → hd.kinopoisk.ru/film/<id>?watch= → РЕАЛЬНОЕ воспроизведение (paused:false, muted:false,
volume 0.51, t растёт, dur 12812с, vis:visible). Навигация через КНОПКУ, не угаданный URL.
Контент-нюанс: не всё в базовом Плюс (Аль-Пачино/Крёстный отец «Оформить подписку» = Амедиатека-
тир; «Однажды в Америке» входит → играет). Это не баг агента.

СДЕЛАНО по фидбеку юзера «хочу сам выбирать сервис / агент сам ищет пока не запустится»:
- ПЕРСИСТЕНТНОСТЬ воспроизведения (act-промпт, не регулярка): не сдаваться с 1-й попытки —
  перебирать результаты/источники (до ~4-5), не помог сервис → пробовать другой, ПОКА не
  подтвердится реальное воспроизведение; не навязывать один сервис; «дай выбрать» → открыть
  выдачу и дать кликнуть самому.
- НЕ УГАДЫВАТЬ URL поиска (корень 404 на anilibria/hd.kinopoisk): промпт+md = открыть ГЛАВНУЮ
  домена и искать через её строку поиска. + ДЕТЕРМИНИРОВАННАЯ 404-RECOVERY в browser_open:
  открытый URL дал 404 → авто-открыть домен-root с подсказкой искать через строку поиска
  (не полагаемся на модель). Проверено: anilibria.tv/search?q= → авто-главная AniLiberty.
- БАГ ПОЧИНЕН: ложный успех «играет фоном: 404» → `_is_error` гейт (анти-ложь).

PLAYBACK-БЮДЖЕТ (2026-06-12, по фидбеку «поправить весь видеоконтент чтобы запускался»):
`_is_play_intent` хелпер (выверенная эвристика для БЮДЖЕТА+анти-лжи, не маршрутизация) →
для playback act получает: rounds_cap 12 (вместо 6), max_nudges 4 (вместо 1, толчок
персистентности «пробуй следующий результат/источник пока не заиграет»), дедлайн ×2. +
промпт «заход через ПОИСК, не застревай на витрине без поиска». Результат: персистентность
довела агента до ПРОФИЛЬНЫХ аниме-сайтов (YouTube→anilibria→jut.su), но полная навигация
free-сайта (главная→поиск→тайтл→серия→плеер с рекламой/анти-ботом) до плеера СТАБИЛЬНО НЕ
доведена — застревает на витрине/промо.

ОТКРЫТО (честно): ФИЛЬМ на подписном сервисе (Кинопоиск) = ✅ надёжно; FREE аниме-сайты =
агент доходит до сайта, но не до плеера (сложная многошаговая навигация + ads + анти-бот).
Самый перспективный левер: (б) для видео ПРЕДПОЧИТАТЬ подписной сервис юзера (Кинопоиск —
там и фильмы, и лицензированное аниме типа Атаки титанов играют надёжным плеером), free-сайты
— фолбэк по явной просьбе. Реализация: сервис-предпочтение видео в памяти (как музыка) +
заход через kinopoisk.ru/поиск. Минус: юзер просил НЕ навязывать Кинопоиск → надёжность как
дефолт + «дай выбрать»/назвать сервис как опция. Также (в) выбор серии/сезона, полный экран.
КЛИК ПО ВИДЕО (v12, 2026-06-12, фидбек «free-сайты в приоритете, не костыль»): обобщённый
ФОЛБЭК — `locatevideo` (центр крупнейшего `<video>`) + trusted CDP-клик по нему в
trustedSoundNudge, ПОСЛЕ кнопки-плей и gesture, ТОЛЬКО если звук не пошёл (рабочие музыка/
фильмы не трогает — регресс подтверждён: Believer играет). Не хардкод сайтов.
jut.su вживую: навигация ✅ (поиск без Enter+клик «Найти» → аниме → серия → плеер-страница).
ПРИЧИНА не-воспроизведения (скриншот юзера): НЕ анти-бот, а ПОДПИСКА — в оверлее плеера «Для
просмотра серии необходимо наличие Jut.su+» (free-сайт загейтил за свой премиум). Агент НЕ
детектил (сигнал в ОВЕРЛЕЕ/read-тексте, не в снапшоте; регекс не знал «необходимо наличие …+»).
ПОЧИНЕНО (по фидбеку «нужна подписка, но он не задетектил»): обобщённый `_PAYWALL_RE`
(premium-гейт `<X>+`, «для просмотра необходимо», «только для подписчиков», апгрейд тира,
вход) проверяется И в снапшоте (ранний отказ), И — при провале дожима — в browser_read() ТЕКСТЕ
страницы (оверлей). Порядок: снапшот-paywall → авто-дожим play → при провале read+paywall →
честный отказ «требует подписки/входа, могу трейлер или где бесплатно». Регекс выверен: ловит
jut.su+/Кинопоиск-подписку, НЕ ложно на играющем плеере/музыке. Живьём: «включи аниме на
jut.su» → честный отказ про подписку (раньше молча «не пошло»). 230 тестов.
v12 клик-по-видео остаётся фолбэком для free-плееров БЕЗ подписки/анти-бота. Okko/ivi и
подписные кинотеатры — стандартный плеер (как Кинопоиск), работают ПРИ наличии подписки.
### H4b. Снапшот-гэп SPA (1) + CDP-слой (2)   [(1) ✅ clicktext, (2) частично]
По фидбеку «1 и 2» (снапшот не выцепляет SPA-дропдауны; нужен CDP-слой). Диагноз anilibria:
результаты поиска в DOM (read видит) и кликаются `.b-search_result a`, но структурный снапшот
их не нумерует (динамический оверлей-дропдаун).
(1) ✅ СДЕЛАНО — `browser_click_text(text)`: находит самый специфичный ВИДИМЫЙ кликабельный
(a/button/role/pointer) элемент по тексту и кликает, минуя нумерацию снапшота. Общее, не
пер-сайт. В content_actions.js (clicktext) → БЕЗ Reload (инжектится свежим; background уже
прокидывает text). Исключает input/textarea (не строку ввода), целится в кликабельный предок.
Живьём ✅: клик «Манга» на Wikipedia → навигация. 230 тестов. Промпт/md: использовать когда
нужного нет в пронумерованном снапшоте.
ОСТАТОК (1): anilibria-autocomplete ЗАКРЫВАЕТСЯ на blur между type_into и click_text (дропдаун
исчезает) — timing конкретного UI, не баг тула. Обходы: навигация через каталог РЕЛИЗЫ
(стабильные ссылки) или клик в дропдауне тем же вызовом. clicktext работает на СТАБИЛЬНЫХ
элементах (большинство SPA-кейсов).
(2) puppeteer-core ExtensionTransport — НАЧАТО ФАЗАМИ (2026-06-13, юзер поставил Node v26).
PHASE 0 ✅ — БАНДЛ собран: `pptr_build/` (package.json+entry.mjs+build.mjs, esbuild), node-
билтины застаблены CJS-Proxy (`node_stub.js` — node-пути puppeteer не исполняются в SW) →
`extension/vendor/puppeteer.bundle.js` (1.9MB ESM, экспортит connect+ExtensionTransport,
загрузка в Node чистая = SW-safe). puppeteer-core 25.1.0: browser-entry
`lib/puppeteer/puppeteer-core-browser.js`. Пересборка: `cd pptr_build && node build.mjs`.
PHASE 1 [в работе] — изолированный `pp_*`-бэкенд в background.js (manifest type:module):
`ppRun` (ExtensionTransport.connectTab→connect→page) + pp_goto/pp_waitclick/pp_type/
pp_clicktext(xpath)/pp_text/pp_ver. Снапшот-путь (handle) НЕ тронут (format «Наше» цел).
УРОК: dynamic import() ЗАПРЕЩЁН в ServiceWorkerGlobalScope (spec) → только СТАТИЧЕСКИЙ
top-level import (офиц. паттерн). v14 = статический, грузится (pp_ver: ExtensionTransport+
connect = function). v15-17: устойчивость (force-detach debugger до/после, таймауты cleanup,
evaluate-клик вместо page.click).
ВАЛИДАЦИЯ (2026-06-13, ИТОГ): что РАБОТАЕТ — puppeteer-core грузится в MV3 SW ✅;
goto/type/text/**waitForSelector** ✅; **evaluate-клик навигирует SPA** (anilibria РЕЛИЗЫ→
каталог за 1с) ✅. Что НЕ — `page.click()` ВИСНЕТ через ExtensionTransport (CDP clickablePoint/
mouse не реализован) → обход `page.evaluate(el.click())` = DOM-клик (как снапшот, но с надёжным
ожиданием). 🎯 ГЛАВНОЕ ДОКАЗАТЕЛЬСТВО: на ЯНДЕКС ЕДЕ (eda.yandex.ru/search?query=лимонад)
Puppeteer **ВИДИТ карточки товаров** («Лимонад Черноголовка Дюшес/Байкал/Тархун 2 л · 150 ₽»),
которые СНАПШОТ НЕ ВЫЦЕПЛЯЛ (живой провал юзера: «карточки не видны после прокруток, 158k
токенов впустую») — `waitForSelector`+рендер решает. Вывод: Puppeteer оправдан для ТЯЖЁЛЫХ
SPA (Еда/Лавка), где снапшот слеп. Остаток: add-to-cart на Я.Еде = кнопка «+» на карточке (клик
по названию не добавляет) — деталь интеграции. anilibria-autocomplete не триггерится ни
снапшотом, ни pp.type (фин-компонент, не вина Puppeteer).
ХРУПКОСТЬ: MV3 SW засыпает → WS отваливается между pp-вызовами (keepalive переподключает).
PHASE 2 ✅ СДЕЛАНО (2026-06-13) — ИНСАЙТ: ценность Puppeteer = ОЖИДАНИЕ рендера (наш снапшот
хорош, но запускался слишком рано на тяжёлых SPA). Реализация (реюз всего, не переписали клик):
v18 `pp_open`/`pp_see` = Puppeteer ждёт networkidle (700мс idle) → отцепляется → НАШ снапшот на
ОТРЕНДЕРЕННОЙ странице (chrome.scripting). `browser_control._route`: при backend=puppeteer|hybrid
op open_url/see идут через pp_open/pp_see (клики/ввод/медиа — по тому же снапшоту, без изменений;
graceful-фолбэк на обычный снапшот при сбое pptr). CLI `/backend extension|puppeteer|hybrid|window`.
🎯 ДОКАЗАНО E2E: backend=puppeteer → снапшот Я.Еды теперь СОДЕРЖИТ пронумерованные товары
([14] Лимонад Черноголовка Дюшес 2 л 150₽ …), которых снапшот-без-ожидания НЕ видел. Агент через
NL «найди лимонад и добавь в корзину» → **РЕАЛЬНО добавил «Лимонад Дюшес Аскания 200₽ 500мл из
ДадАли» в корзину**, оплата не тронута (граница оплаты). Это закрыло исходный провал (карточки
невидимы → 158k токенов впустую). Перепроверить живьём не успели — юзер закрыл браузер, но
результат конкретен (магазин/объём/цена) = заземлён.
ХРУПКОСТЬ (остаток): MV3 SW засыпает → WS отваливается между pp-вызовами (keepalive
переподключает, но multi-step может дёрнуться); page.click() виснет через ExtensionTransport
(обход — evaluate-клик/наш снапшот-клик). Phase 3 (3 формата + сравнение) — по слову юзера.
Дефолт backend остаётся extension (puppeteer — opt-in через /backend, для тяжёлых SPA;
причина: pp_open/pp_see цепляют debugger → дебаг-полоса мигает на КАЖДОМ open/see + латентность
networkidle — не нужно для лёгких музыка/видео).

ДОБОРКА еды (2026-06-13, фидбек после 1-го успеха): 2-й прогон нашёл РЕСТОРАННЫЕ позиции
(FRENCH BAKERY/Мейхана/Папа Джонс) — там товар не добавляется из общей выдачи (надо открыть
ресторан→меню), а DOM-клик React-кнопки «+» не триггерил. Фиксы (2)+(3):
(2) browser_control.md: Я.Еда МАГАЗИН/Лавка (прямой «+») vs РЕСТОРАН (открыть→меню); для
«положи в корзину <продукт>» предпочитать магазин/Лавку.
(3) `browser_tap(item)` = TRUSTED-клик по номеру (content_actions `coordnum`→центр элемента +
background `tap`→cdpClick): React-кнопки, игнорящие el.click(), реагируют на настоящий жест.
v19. browser_control.md: «+» не сработала DOM-кликом → browser_tap. 234 теста.
ЖДЁТ Reload v19 + повтор еды (backend=puppeteer выставлен). 1-й прогон уже доказал добавление
магазинного товара; (2)+(3) — на надёжность по ресторанам/React-кнопкам.
Дальше Phase 2 (hybrid: pp для трудного SPA + наше для рабочего), Phase 3 (3 бэкенда,
browser_backend=puppeteer|hybrid|extension, сравнение _before_after_eval).
- сайты-каталоги, сервис-агностично через меню/поиск.

### DE-CRUTCH: маршрутизация browse — из регулярки в КЛАССИФИКАТОР   ✅ (2026-06-12, по замечанию юзера «регулярки = костыли»)
Юзер справедливо указал: keyword-регулярка `_wants_current_web` подменяла решение reflexion-
классификатора — это костыль против принципа «решения универсальны, не костыльны» и против
механизма обучения (few-shots/бандит на выбор режима). УБРАНО: `_wants_current_web` +
`_WEB_FIND_RE`/`_WEB_CONTENT_RE` удалены. Вместо них — ПРИНЦИПИАЛЬНО:
- reflexion-промпт: «BROWSE = тоже act» (найти/перечислить актуальный контент = лёгкая
  многошаговость act, не deliberate) + grounding-честность (текущую выдачу НЕЛЬЗЯ из памяти →
  grounding низкий → существующий GROUNDING_GATE заземляет). Классификатор решает сам, few-shot
  обучение корректирует;
- `_skills_for_act` ВСЕГДА добирает web_search (+browser_control+device_control) → LLM сам
  выбирает read-vs-physical в лёгком цикле, без keyword-форса.
Проверено: тот же find-видео кейс БЕЗ регулярки → классификатор сам в act, бюджет цел, 6
реальных тайтлов. 230 зелёных.
ОСТАЛИСЬ защитные сетки вывода (`_is_degenerate`/`_is_false_access_refusal`/`_is_meta_ack`):
это НЕ маршрутизация, а дешёвое детерминированное ЭНФОРСМЕНТ жёсткого требования «не
галлюцинировать» (ловят, когда модель нарушает промпт под давлением бюджета). Принципиальный
дом для них — нода `validation` (LLM-гейт качества), но act/browse её не проходит (цена).
Кандидат: перенести в validation/лёгкую honesty-проверку — по слову юзера.

### H5. Еда / заказы   [В РАБОТЕ — граница оплаты ✅ 2026-06-13]
Многошаговый НЕОБРАТИМЫЙ сценарий: поиск ресторана/блюда → выбор → корзина → оформление.
ГЛАВНОЕ (деньги): ДВЕ детерминированные границы, агент НЕ пересекает их сам в ЛЮБОМ режиме
(включая auto — иначе сохранённая карта = заказ в один тап без спроса):
1. ПОЛЯ карт/паролей/документов — `browser_type` отказывается печатать (_SENSITIVE_JS, было).
2. ✅ НОВОЕ: КНОПКА оформления/оплаты («Оформить заказ», «Оплатить N ₽», «Заказать за N ₽»,
   «Подтвердить заказ», place order/pay now/checkout) — `browser_click`/`browser_click_text`
   в content_actions ВОЗВРАЩАЮТ «[ГРАНИЦА ОПЛАТЫ]» и НЕ нажимают (helpers payLabel/payRefusal,
   _PAY_RE). Агент готовит корзину → показывает итог → ask_user, человек жмёт финальную кнопку
   САМ. Regex выверен: блокирует пэй-кнопки, пропускает «В корзину/Заказать/Выбрать». Без
   reload (content_actions свежий). Тесты: test_service_choice (+payment_boundary). 232 зелёных.
ИНЦИДЕНТ (2026-06-13, «закинь суши в корзину на niyama.ru») — 3 БАГА ОРКЕСТРАЦИИ, ПОЧИНЕНЫ:
1. МИСРОУТ: браузер-задача ушла в create_skill → агент стал СТРОИТЬ Python-навык, тот импортил
   пакет → `uv add` (cryptography + 43 пакета, timeout 300с) ЗАМОРОЗИЛ задачу на минуты. Фикс:
   router-промпт «ФИЗИЧЕСКИЙ ВЕБ = ВСЕГДА use_skills (browser_control), НИКОГДА create_skill,
   не ставь зависимости». + `ensure_python_package` timeout 300→120с с честным сообщением.
2. ⚠️ БЕЗОПАСНОСТЬ: агент исказил домен → открыл niama.ru (ВРЕДОНОСНЫЙ тайпсквот) вместо точного
   niyama.ru. Фикс ДВ slой: (а) промпт-правило «точный URL побуквенно, niyama≠niama, фишинг»;
   (б) ДЕТЕРМИНИРОВАННАЯ защита `browser_bridge.safe_url`+`set_user_domains` (recall): домены
   из запроса юзера; открываемый близкий-но-другой (edit-distance 1-2) → правится на ТОЧНЫЙ
   пользовательский (niama→niyama). Тест: anti_typosquat. Точный/другой/без-доменов не трогаем.
3. ГРАНИЦА ОПЛАТЫ (уже была H5): финальную кнопку заказа агент не жмёт сам.
Урок: МНОГОШАГОВЫЙ БРАУЗЕР-ФЛОУ на одном сайте = browser_control (navigate), НЕ создание навыка
и НЕ установка зависимостей. Орк теперь: reflexion→deliberate→router(use_skills)→browser_control
→ открыть ТОЧНЫЙ домен → навигировать → корзина → [ГРАНИЦА ОПЛАТЫ]. 234 теста.
Живьём (browse): «покажи рестораны с пиццей в Я.Еде» → eda.yandex.ru/moscow, нашёл пиццерии
(адрес уже задан, юзер залогинен). НО: ушло в deliberate, бюджет 129k>120k (eda тяжёлая) →
обрезано, ответ мог смешать память (Dodo/Papa John's) с реальной выдачей (Pizza Heart/Hut) —
тот же heavy-сайт+бюджет лимит, что у видео. Догнать: browse-роутинг (act, не deliberate) для
еды; реальный e2e заказа ДО границы оплаты (корзина); адрес/время доставки как обратимые шаги.

### H6. «Лёгкая многошаговость» (browse-режим)   ✅ СДЕЛАНО (2026-06-12)
Диагноз (подтвердился): бюджет жгла НЕ длина снапшотов (act уже их режет: `_DIRECT_ROUNDS=6`,
`TOOL_OUTPUT_CAP=1500`), а МАШИНЕРИЯ deliberate (decompose + шаги с большими промптами +
synthesize + validation = 120-166k). Юзер: «deliberate по сути heavy; мб лёгкую многошаговость».
Решение БЕЗ нового режима: browse = существующий act (он и есть лёгкий ReAct ≤6 раундов с
обрезанными наблюдениями). Что сделано:
1. reflexion-гейт `_wants_current_web` («найди/покажи/перечисли» контента) → форс в **act**
   (даже если модель выбрала deliberate — act эскалирует обратно, если шагов не хватит:
   лестница act→deliberate, принцип F). heavy не трогаем.
2. `_skills_for_act` добирает **web_search** для browse-запросов → в лёгком цикле LLM сам
   выбирает headless-чтение (web_search) vs физ-браузер (browser_*) по read-vs-physical.
3. act-промпт: разрешена многошаговость для поиска/чтения выдачи (не «1-2 вызова»); ИТОГ
   обязан СОДЕРЖАТЬ результат целиком (юзер не видит промежуточные шаги).
4. `_is_meta_ack` + ресинтез в `_exec_direct`: «я перечислил/список выше» без самого списка
   → повторный запрос «выдай результат ЦЕЛИКОМ из данных инструментов» (живой баг browse).
Живьём: «найди на YouTube обзоры Sony XM5 и перечисли названия» → act, БЮДЖЕТ ЦЕЛ, финал =
7 реальных тайтлов с деталями (раньше: 125k+, обрезано до 1 строки / выдумка). «найди обзоры»
без сайта → headless web_search дёшево. Музыка/воспроизведение регресс цел (Blinding Lights).
Тесты: test_service_choice (+meta_ack), 229 зелёных. Это и фундамент многошаговой еды (H5).

## I. СТРАТЕГИЯ: устройство-агент (мозг / WASM-среда / per-OS руки) + платформенный риск   [ОРИЕНТИР, задан юзером 2026-06-13]

ВИДЕНИЕ юзера (уточнённое): не браузер-расширение, а УСТАНАВЛИВАЕМОЕ ПРИЛОЖЕНИЕ на устройства
(часы/телефон/ПК), которое ПОСЛЕ ВЫДАЧИ ПРАВ работает с СИСТЕМНЫМИ приложениями (открыть
браузер на часах, вытащить данные тренировки, проанализировать; на ПК/смартфоне — так же). У
приложения СОБСТВЕННАЯ исполняемая среда с кодом, поставляемая и поддерживаемая на девайсах.

ТРИ СЛОЯ (их судьба у платформ — разная):
1. МОЗГ — LLM (на устройстве маленький / сервер; тонкий клиент — у нас server.py).
2. СРЕДА ИСПОЛНЕНИЯ навыков — код, поставляемый в приложении. КЛЮЧ: обе платформы ЗАПРЕЩАЮТ
   докачку/исполнение НЕЙТИВНОГО кода (Apple 2.5.2; Google Play — запрет DexClassLoader удалённого
   кода, как MV3 убил remote-code). → ОТВЕТ: **WASM-песочница, поставляемая в приложении**;
   навыки = данные/байткод (не нативный бинарь). Единая на любой OS, легальна у всех. Сюда
   ложатся «3 слоя защиты (AST→песочница→HITL)»: WASM = изолированная песочница, права OS+HITL =
   ворота для рук.
3. РУКИ — доступ к системным приложениям/данным. ГЛАВНЫЙ РАСКОЛ Google ≫ Apple:
   - GOOGLE/Android/Wear — относительно ОТКРЫТО: AccessibilityService = универсальная рука
     (читать экран+клики/жесты в любом приложении), НО Play режет accessibility «не по
     назначению» (боты-автоматизаторы удаляют) → полная мощь через SIDELOAD/APK/enterprise
     (вне ревью Play); Health Connect = САНКЦИОНИРОВАННЫЙ хаб данных тренировки (берём отсюда с
     разрешением, не скрейпинг); Intents/foreground-service штатно; локальный твой код ок,
     докачка нативного — нет.
   - APPLE/iOS/watchOS — ЖЁСТКО ЗАКРЫТО (стена): нет third-party accessibility-автоматизации;
     меж-приложенческое — ТОЛЬКО App Intents/Shortcuts/Siri (приложения сами ЭКСПОРТИРУЮТ
     действия, агент оркестрирует); HealthKit для данных; watchOS — компаньон телефона+HealthKit;
     Apple строит СВОЙ агент (App Intents+Apple Intelligence) — третьи лица по его рельсам.

ЧТО ЛУЧШЕ (вывод):
- Исполняемую среду — на WASM (одна песочница везде, проходит правила обеих).
- Руки — сменные per-OS АДАПТЕРЫ за ЕДИНЫМ интерфейсом (как наш `browser_*` за роутером
  бэкендов): accessibility-android, app-intents-ios, accessibility-macos/uia-windows,
  healthconnect/healthkit для данных, browser-extension на десктопе.
- Перс-данные (тренировки/здоровье) — из САНКЦИОНИРОВАННЫХ API (Health Connect/HealthKit), не
  скрейпингом: безопаснее И платформо-легально (усиливает «безопасный повседневный»).
- Реализм охвата: полная универсальность системными приложениями достижимее на Android/Wear/
  десктопе (+sideload-тир для мощи); на iOS/watchOS — по рельсам Apple. Это ПОТОЛОК платформы,
  не недоработка.
ГДЕ МЫ СЕЙЧАС в этой модели: наш browser-extension = десктоп-браузерная РУКА; sandbox
(AST→песочница→HITL) = прообраз WASM-среды; server.py = тонкий клиент. Движок взаимодействия
(снапшот/chrome.debugger/Puppeteer) держать СМЕННЫМ (3 бэкенда) — страховка от платформенных
закрутов: переключаешь бэкенд, а не переписываешь агента.

## Сделано (контекст)

- БЗ юзера: `knowledge_base.py` + настоящий LightRAG (`lightrag_engine.py`), иерархия папок,
  ярус-3 сессионные файлы, CLI `/kb`, `/attach`.
- Hardening AutoRAG (2026-06-12): sanitize в recall (отравленный документ ≠ команды);
  авто-впрыск = дешёвый BM25, граф за явным тулом; структурный флаг `own_docs` вместо
  фраз-сентинелей; анти-path-traversal; `list_kb` с иерархией.
- `/kb add` больше не тратит молча: `estimate_index_cost` (прикидка чанков/вызовов/$) +
  HITL-вопрос; отказ → документ в бесплатный BM25 (`use_graph=False`).

## Не сейчас

- Кроссплатформенное приложение «любая OS»: `server.py` уже даёт мозг на сервере, тонкие
  клиенты — после докрутки контура.
- Дорогие eval-прогоны (GAIA и т.п.) — только по явному слову юзера.
