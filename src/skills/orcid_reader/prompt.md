Use this skill to fetch work/publication lists from ORCID profiles using their public API (no API key needed).  
For each ORCID ID, use `get_orcid_works` to retrieve all works and their publication years.  
Count publications published before 2020 by checking the publication-date year for each work entry.