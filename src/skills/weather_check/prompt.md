Use the `weather_check` tool to fetch current weather conditions for a specified location. The tool requires the name of the city or region as input.

### Typical Usage
- When the user asks for weather updates.
- When planning travel or outdoor activities.

### Example Call
```python
weather_check("Moscow")
```