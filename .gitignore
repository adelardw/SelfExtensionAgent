# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
logs

# Secrets / runtime data
.env
/data/
config.local.yml
.sea/
data/browser_bridge.token
data/browser_bridge.log

# intent-роутер: кодбук = runtime-состояние (seed-кэш + выученные экземпляры), как memory.db
data/intent_codebook.json
data/route_examples.db

# frontend (Vite/React)
frontend/node_modules/
frontend/dist/

# PDB files (debug symbols)
*.pdb

# рабочий план — только локально, вне git-истории
develop.md

# Артефакты, произведённые агентом для отдачи юзеру (export_table) — генерируются в рантайме
/artifacts/
src/skills/.emb_cache.json
/tmp/sim_chat_threads/
data/eval/harness_runs.jsonl
